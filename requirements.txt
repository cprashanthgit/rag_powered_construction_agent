# ══════════════════════════════════════════════════════════════════════
# requirements.txt — Streamlit Cloud (UI frontend only)
#
# app.py is a pure HTTP frontend that calls the GCP backend API.
# It only needs streamlit and httpx — no ML packages needed here.
#
# Full ML/RAG dependencies (faiss, torch, langchain, etc.) are in:
#   requirements-api.txt  → used by deploy/Dockerfile on GCP VM
# ══════════════════════════════════════════════════════════════════════

streamlit>=1.40.0
httpx>=0.27.0
